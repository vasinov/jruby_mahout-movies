$(document).ready ->
  $('.star').raty( {
    path: "/assets/",
    score: ->
      return $(this).attr('data-rating')
    ,
    click: (score, evt) ->
      $.post "/movies/" + $(this).parent().data("movie_id") + "/rate/" + score + ".json", "json"
  })